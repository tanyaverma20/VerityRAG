fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
python-dotenv==1.0.1

# Retrieval
chromadb==1.5.9
fastembed>=0.4.0
rank-bm25==0.2.2
sentence-transformers==3.1.1

# LLM
groq==0.11.0

# Document parsing
pypdf==5.0.1

# Eval
pandas==2.2.3

# Phase 3 Orchestration
langgraph==0.2.14
langchain-core==0.2.33

transformers==4.57.6
tokenizers==0.22.2

# Report generation (Markdown/PDF/DOCX export from validated JSON — no LLM formatting)
reportlab==5.0.0
python-docx==1.2.0

# Structured application persistence (db/ package). DATABASE_URL selects
# PostgreSQL in production; falls back to the same SQLite file used before
# when unset, so local dev/tests need nothing extra installed at the OS level.
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.19.1

# Production cache (cache.py). Falls back to the existing in-memory cache
# automatically if REDIS_URL is unset or the Redis server is unreachable.
redis==8.1.0

# OCR fallback for scanned/image-only PDFs (ingest.py). Optional at runtime:
# requires the Tesseract OCR system binary to actually be installed: only
# invoked when a PDF's normal text extraction comes back empty/insufficient.
pytesseract==0.3.13

# PDF page/figure rendering for Explain Figure's image-based path
# (analysis.py). Already a transitive dependency of some installs; pinned
# explicitly here since it's now used directly.
PyMuPDF==1.28.0